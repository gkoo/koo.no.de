h2.sectionTitle welcome to the grid
.loading
  p.loading-status Initializing grid particles...
  ul#grid
    - for (var i=0; i<dimSize; i++)
      li
        ul.innergrid
          - for (var j=0; j<dimSize; j++)
            li
  #controls
    #colorMsg
      div.black
      p Your color
    ul.colors
      li
        a.color.black(href='#')
      li
        a.color.red(href='#')
      li
        a.color.green(href='#')
      li
        a.color.blue(href='#')
      li
        a.color.yellow(href='#')
      li
        a.color.white(href='#')
    p
      span#username
        span
        input#editNameBtn(type='button', value='Edit your name')
      span#editNameField
        input#editNameText(type='text')
        input#editNameSubmit(type='button', value='Save name')
      input#clearBtn(type='button', value='Clear the Grid')
      //- input#testbtn(type='button', value='print grid on server')
    //-
      h2 Artists
      ul
        li.null Where is everyone? There's nobody here!
.extra
  .whatisthis
    h2 What is this?
    p
      | This project is just an experiment with the real-time
      | capabilities of
      a(href="http://nodejs.org", target="blank") NodeJS
      |  using
      a(href="http://socket.io", target="blank") socket.io
      | .
    h2 What do I do here?
    p
      | Use your mouse to click and drag on the grid to draw on it.
      | What you draw will show up in other people's browsers, so
      | try it with a friend! If you don't happen to have a friend
      | around at the moment, you can still try it out by opening
      | up this same page in two browsers and seeing your drawing
      | appear in both browsers at the same time!
  .upcoming
    h2 Upcoming features!
    ul
      li Saving!
      li Brush size! (maybe!)
      li Mobile support!
      li That's it!

script(src='/socket.io/socket.io.js')
script(src="/js/jquery-1.7.1.min.js")
script(src="/js/underscore-min-1.3.1.js")
script(src="/js/backbone-min-0.9.1.js")
script(src='/js/gridclient.js')
